int Inside(double x, double a, double b);
int Inside(double x, double a, double b) {
	int ans = 0;
	ans = (x > b) ? 1 : ((x < a) ? -1 : ans);
}

//Эта функция проверяет, находится ли x внутри интервала (a, b).
//Если x больше b, функция возвращает 1.
//Это означает, что x находится за пределами интервала.
//Если x меньше a, функция возвращает -1, что также означает,
//что x находится за пределами интервала.
//В противном случае функция возвращает 0, указывая, что x находится внутри интервала.